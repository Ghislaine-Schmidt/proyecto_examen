<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @building.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @building.address %>
</p>

<p>
  <strong>City:</strong>
  <%= @building.city %>
</p>

<ul class="list-group">
  <% @building.apartments.each do |apartment| %>
    <li class="list-group-item"><%= apartment.referencia %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_building_path(@building) %> |
<%= link_to 'Back', buildings_path %>

<%= link_to 'agrega un depto', new_building_apartment_path(@building) %>

